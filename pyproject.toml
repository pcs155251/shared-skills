[project]
name = "shared-skills"
version = "0.1.0"
description = "Shared environment for all skills in this repo"
requires-python = ">=3.11"
dependencies = [
    "python-pptx>=1.0",
    "pandas>=2.0",
    "openpyxl>=3.1",
    "matplotlib>=3.8",
]
